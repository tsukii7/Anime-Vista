> 🌐 查看英文版: [English](./README.md)

## 🧱 项目结构

本项目结构按职责划分，采用 Model-View-Presenter 架构。

前端UI与项目文件对应关系详见 [`relationship.pptx`](./relationship.pptx)。

```text
anime-vista/
├── .firebase/
│   └── ...
├── .husky/
│   ├── _/
│   │   └── ...
│   └── commit-msg
├── .react-router/
│   └── ...
├── build/
│   └── ...
├── docs/
│   ├── api/
│   │   └── ...
│   ├── architecture/
│   │   └── ...
│   ├── contribution/
│   │   └── ...
│   ├── dev-guide/
│   │   └── ...
│   ├── firebase/
│   │   └── ...
│   ├── routing/
│   │   └── ...
│   └── state/
│       └── ...
├── functions/
│   ├── build/
│   │   └── server/
│   │       └── index.js
│   ├── .gitignore
│   ├── index.js
│   └── package.json
├── public/
│   └── ...
├── scripts/
│   └── copySSR.mjs
├── src/
│   ├── app/
│   │   └── store.js
│   ├── assets/
│   │   └── ...
│   ├── components/
│   │   └── ...
│   ├── firebase/
│   │   └── ...
│   ├── models/
│   │   ├── authentication/
│   │   │   └── ...
│   │   ├── current/
│   │   │   └── ...
│   │   ├── details/
│   │   │   └── ...
│   │   ├── developer/
│   │   │   └── ...
│   │   ├── home/
│   │   │   └── ...
│   │   ├── me/
│   │   │   └── ...
│   │   ├── rank/
│   │   │   └── ...
│   │   ├── search/
│   │   │   └── ...
│   │   └── user/
│   │       └── ...
│   ├── presenters/
│   │   └── ...
│   ├── routes/
│   │   ├── entry.client.jsx
│   │   ├── entry.server.jsx
│   │   ├── root.jsx
│   │   └── routes.js
│   ├── styles/
│   │   └── ...
│   └── views/
│       ├── About/
│       │   └── ...
│       ├── Current/
│       │   └── ...
│       ├── Details/
│       │   └── ...
│       ├── Home/
│       │   └── ...
│       ├── LoginRegister/
│       │   └── ...
│       ├── Me/
│       │   └── ...
│       ├── Rank/
│       │   └── ...
│       └── Search/
│           └── ...
├── .env
├── .firebaserc
├── .gitignore
├── commitlint.config.cjs
├── eslint.config.js
├── firebase.json
├── index.html
├── package.json
├── package-lock.json
├── react-router.config.js
├── README.md
├── README.zh-CN.md
├── server.js
└── vite.config.js
```

---

### 📁 `.firebase/`

该目录为 Firebase CLI 在初始化项目和部署时自动生成的本地缓存目录，主要用于存储与 Firebase Hosting 相关的临时文件，应被
`.gitignore` 排除。

---

### 📁 `.husky/`

用于配置 Git Hooks，配合 [Husky](https://typicode.github.io/husky/) 工具自动化执行特定 Git 操作前的校验逻辑，提升代码质量。

- [`_/`](../../.husky/_/): Husky 内部生成的辅助脚本目录，通常包含环境变量初始化逻辑，不需要手动修改。
- [`commit-msg`](../../.husky/commit-msg): 在每次提交时触发，调用 `commitlint` 检查提交信息是否符合 Angular
  提交规范，防止提交不规范的记录。

Husky 钩子可扩展为 `pre-commit`、`pre-push` 等，当前项目仅启用了 `commit-msg` 用于校验提交格式。

---

### 📁 `.react-router/`

由 `@react-router/dev` 在构建过程中自动生成的缓存目录，包含路由模块的构建中间状态与临时产物。

- 通常不需要手动修改或查看
- 会在每次执行 `react-router build` （该项目内配置为 `npm run build`） 时重新生成
- 已加入 `.gitignore`

---

### 📁 `build/`

服务端渲染（SSR）构建输出目录，包含客户端和服务端构建产物。
该目录由 `react-router build` （该项目内配置为 `npm run build`）命令生成，部署应用时需依赖此目录。

- `build/client/`：浏览器端可访问的静态资源（JS、CSS、图片等），由 Vite 编译而成
- `build/server/`：服务器端用于响应请求的 SSR 模块，入口为 `build/server/index.js`
- 无需手动修改，已加入 `.gitignore`

---

### 📁 `docs/`

存放文档资料，如架构说明、组件说明、API 文档等。

编写规范：

1. 每个主题各新建一个文件夹，文件夹内包含md文件和其他所需文件
2. 主要文件分中英两版，英文版命名为 `README.md`，中文版命名为 `README.zh-CN.md`。其他文件可按需命名
3. 文件内各标题可用一个 emoji 作为前缀，表示该文件的主题，以减少视觉疲劳

---

### 📁 `functions/`

该目录用于存放 Firebase Cloud Functions 相关代码，由 Firebase 部署命令（`firebase deploy`）打包上传至 Cloud Functions，并作为
SSR 服务的后端逻辑执行环境。

---

### 📁 `public/`

用于存放不会被构建工具（Vite/Webpack）处理的静态资源的文件夹。所有放在这里的资源会在打包时原样复制到最终构建目录中，并通过根路径 /
直接访问。

常见用途包括：

- 被 [`index.html`](../../index.html) 直接使用的文件
- `favicon.ico`：浏览器标签页图标
- 静态图像资源：`/logo.png` 可通过 `<img src="/logo.png" />` 引用
- SEO 配置文件（如 `robots.txt`, `manifest.json`）

📌 该目录禁止存放动态组件中引用的图像资源，它们应被放于 `src/assets/`。

---

### 📁 `scripts/`

该目录存放项目构建与部署过程中使用的自定义 Node.js 脚本，属于开发者工具目录。该目录下的脚本通常不参与项目运行，但能提升自动化与部署效率。

目前包含的文件有：

- [`copySSR.mjs`](../../scripts/copySSR.mjs)：构建完成后自动将 `build/server/` 中的服务端产物复制到
  `functions/build/server/`，用于 Firebase Functions 的部署。该脚本会自动识别当前操作系统（Windows 或
  macOS/Linux），并执行适配的复制命令，确保跨平台兼容性。

---

### 📁 `src/`

前端源代码主目录，所有 React 相关代码、资源与业务逻辑均存放于此。

本项目采用 **Model-View-Presenter（MVP）** 架构，：

- 🧠 Model（模型层） → [`src/models/`](../../src/models)
    - 负责管理应用状态，基于 Redux Toolkit 建立的 `slice` 模块
    - 每个页面或业务模块有独立的状态模型
    - 所有数据更新逻辑封装在 Reducers 中，避免散落在视图组件中

- 🎨 View（视图层） → [`src/views/`](../../src/views) + [`src/components/`](../../src/components)
    - 负责 UI 展示和用户交互，不直接处理业务逻辑或状态更新
    - 页面组件位于 [`views/`](../../src/views)，每个页面一个独立子目录（如 [`Home/`](../../src/views/Home), [
      `Current/`](../../src/views/Current)）
    - 可复用组件（如卡片、表单项、布局）统一放在 [`components/`](../../src/components)

- 🧭 Presenter（逻辑协调层） → [`src/presenters/`](../../src/presenters)
    - 负责协调 Model 与 View 的交互（如提交筛选条件、处理事件）
    - 处理复杂业务流程（例如分页、请求数据、组合多个状态）
    - 视图组件通过调用 Presenter 中的方法来间接修改状态

#### 📁 `src/app/`

全局配置目录，包括：

- [`store.js`](../../src/app/store.js)：Redux Store 配置

#### 📁 `src/assets/`

用于存放由 React 组件引用的资源，例如图片、图标、字体等。构建工具会自动对这些资源进行打包、压缩、版本控制等优化。

引用方式通常是使用 import 语句，例如：

```js
import loading from '../assets/loading.gif';

<img src={loading} alt="loading"/>;
```

适用于：

- React 组件中使用的图片（封面、头像、图标等）
- 可被按需加载的资源（比如动态主题图标）
- 组件内使用的字体、SVG 等前端静态文件

📌 该目录禁止存放直接被 [`index.html`](../../index.html) 使用的图像资源，它们应被放于 [`public/`](../../public)。

#### 📁 `src/components/`

可复用组件集合，按功能模块进一步划分子目录：

- [`card/`](../../src/components/card)：被复用的卡片样式，如 [`ListCard`](../../src/views/Current/components/ListCard.jsx)
  和 [`ResultCard`](../../src/views/Search/components/ResultCard.jsx) 样式相同，故提取为可复用的 [
  `IntroCard`](../../src/components/card/IntroCard.jsx) 组件
- [`layout/`](../../src/components/layout)：布局相关组件，如 `TopBar`、`SideBar` 等

#### 📁 `src/firebase/`

封装与 Firebase 平台交互的所有逻辑，统一管理配置、认证与数据库操作，避免在组件或 presenter 中直接调用 Firebase API，符合
“关注点分离” 的架构原则。

- [`config.js`](../../src/firebase/config.js): 初始化并导出 Firebase App 实例，配置项（如 API Key）通过 `.env`
  文件注入。只在项目中初始化一次。

- [`auth.js`](../../src/firebase/auth.js): 封装与 Firebase Authentication 相关的方法，例如注册、登录、登出、获取当前用户等。

- [`db.js`](../../src/firebase/db.js): 封装与 Firestore 数据库交互的函数，如增删改查、获取收藏番剧、提交用户数据等。

#### 📁 `src/models/`

负责管理全局状态，按业务模块进一步划分子文件夹：

- `authentication/`：对应 `Login` 和 `Register` 页面，存放用户认证相关状态
- `current/`：对应 `Current` 页面，存放当前季度番剧和时间轴相关状态
- `details/`：对应 `Details` 页面，存放一部番剧的基本信息、角色、声优等详情内容
- `developer/`：对应 `About us` 页面，存放开发者相关信息，为固定文本
- `home/`：对应 `Home` 页面，存放首页推荐番剧相关状态
- `rank/`：对应 `Rank` 页面，存放番剧排行榜相关状态
- `search/`：对应 `Search` 页面，存放筛选、搜索、结果相关状态
- `user/`：对应 `Me -Favorites` 和 `Me -Activities` 页面，存放用户收藏和动态相关状态

该目录下子文件夹按主题命名，使用小写字母，优先考虑可读性，不一定与页面名称相同

#### 📁 `src/presenters/`

中间业务逻辑调度层，协调 View 与 Model 之间的交互，保证解耦。

一个页面有且只有一个 Presenter，负责该页面的所有业务逻辑。

#### 📁 `src/routes/`

集中管理 React Router 路由系统的所有核心文件。该目录下文件用于配置项目的页面结构、入口组件与客户端初始化，支持服务端渲染（SSR）和路由预加载。

- [`routes.js`](../../src/routes/routes.js): 使用 `@react-router/dev` 提供的声明式路由 API 定义所有页面的路由结构。每个
  route 指向一个对应的 Presenter，实现解耦与逻辑分离。
- [`root.jsx`](../../src/routes/root.jsx): 顶层布局组件（Layout），包含 `TopBar`、`SideBar` 等全局组件，并通过 `<Outlet />`
  渲染子路由内容。
- [`entry.client.jsx`](../../src/routes/entry.client.jsx): 客户端入口文件，用于挂载 React 应用并初始化 React Router
  的客户端导航逻辑，结合 `hydrateRoot` 实现 SSR 客户端激活。

#### 📁 `src/styles/`

全局样式，内含文件：

- [`global.css`](../../src/styles/global.css)：全局样式
- [`LoginRegister.css`](../../src/styles/LoginRegister.css)：由于登录和注册界面共用一个 Presenter、其样式被两个 View 共享，故将其亦置于该全局样式目录下

#### 📁 `src/views/`

页面级视图组件，每个子目录对应一个页面，子目录按照页面名称命名，首字母大写。

- [`Home/`](../../src/views/Home)：首页
- [`Current/`](../../src/views/Current)：本季页面（List / Timeline）
- [`Search/`](../../src/views/Search)：筛选搜索页面
- [`Rank/`](../../src/views/Rank)：番剧排行榜
- [`Details/`](../../src/views/Details)：番剧详情页
- [`LoginRegister/`](../../src/views/LoginRegister)：登录/注册页面
- [`Me/`](../../src/views/Me)：用户个人中心
- [`About/`](../../src/views/About)：开发者信息页

每个子目录包含：

- `[前缀]View.jsx` ：一般只有一个。

  若页面存在切换视图，则会一个`[页面名]View.jsx`作为主视图，若干`[页面名][子视图后缀]View.jsx`
  作为子视图，主视图判断当前展示哪个页面。目前有多视图文件的页面有：[`Current/`](../../src/views/Current) 和 [
  `Me/`](../../src/views/Me)。

- `components/`：仅在该页面内使用的组件，其内部可以存在子目录和若干文件，文件和子目录必须首字母大写，其余命名规则有负责该页面的开发者决定。

- `xxx.module.css`：

  页面内使用的 CSS 模块文件，命名规则为 `[组件名].module.css`，并与该组件放置于同目录下（可能在组件目录下，也可能在
  `components`子目录下）。

  该后缀用于启用 CSS Modules 局部作用域机制：使每个组件的 CSS 类名在编译时被自动转换为独一无二的名称，使样式仅作用于当前组件，避免与其他组件发生冲突。

- 以 [`Current/`](../../src/views/Current) 为例：
    ```txt
    ├── Current/                        # 名为 Current 的页面对应的目录
    │   ├── components/                 # 仅用于该页面的组件
    │   │   ├── ListCard.jsx
    │   │   ├── Timeline.jsx
    │   │   └── TimelineCard.jsx
    │   ├── CurrentListView.jsx         # 该页面视图下名为 List 的子视图
    │   ├── CurrentTimelineView.jsx     # 该页面视图下名为 Timeline 的子视图
    │   ├── CurrentView.jsx             # 该页面视图下的主视图
    │   └── CurrentView.module.css      # 仅用于 CurrentView.jsx 组件的 CSS 模块文件
    ```

---

### 📄 根目录其他文件说明

| 文件名                                                      | 内容介绍                                                             |
|----------------------------------------------------------|:-----------------------------------------------------------------|
| [`.env`](../../.env)                                     | 环境变量配置文件，用于存放私密信息（如 Firebase API Key 等），可通过 `import.meta.env` 使用 |
| [`.firebaserc`](../../.firebaserc)                       | Firebase 项目配置文件，记录当前项目关联的 Firebase 项目 ID 和别名，用于部署时识别目标项目         |
| [`.gitignore`](../../.gitignore)                         | Git 忽略文件清单，列出哪些文件/文件夹在提交时不应被包含（如 `node_modules`、`.env` 等）        |
| [`commitlint.config.cjs`](../../commitlint.config.cjs)   | Commitlint 配置文件，配合 Husky 检查 Git 提交信息是否符合 Angular 提交规范，提升提交规范性    |
| [`eslint.config.js`](../../eslint.config.js)             | ESLint 配置文件，用于定义代码风格和语法检查规则，保持团队代码一致性                            |
| [`firebase.json`](../../firebase.json)                   | Firebase Hosting 和 Functions 的部署配置文件，定义部署目录、重写规则、函数入口等           |
| [`index.html`](../../index.html)                         | 应用的 HTML 入口文件，Vite 会将 React 应用注入到这里的 `<div id="root">` 中         |
| [`package.json`](../../package.json)                     | Node 项目配置文件，定义了项目依赖、脚本命令、元信息等                                    |
| [`package-lock.json`](../../package-lock.json)           | 依赖锁定文件，记录了确切版本，保证团队间安装依赖一致                                       |
| [`react-router.config.js`](../../react-router.config.js) | React Router 配置文件，用于自定义路由构建逻辑（如定义入口文件、SSR 模式、构建路径等）              |
| [`README.md`](../../README.md)                           | 项目说明文档，介绍项目用途、运行方式、使用技术等（对外/对内说明）                                |
| [`README.zh-CN.md`](../../README.zh-CN.md)               | 中文版项目说明文档，方便中文用户理解项目结构和使用方式，内容与 `README.md` 英文版相对应               |
| [`server.js`](../../server.js)                           | 用于本地测试 SSR 功能，无需部署至云端，可通过 `npm start` 命令直接启动                     |
| [`vite.config.js`](../../vite.config.js)                 | Vite 构建工具的配置文件，配置别名、代理、插件等内容                                     |

